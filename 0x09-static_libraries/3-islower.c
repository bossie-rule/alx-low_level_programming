#include "main.h"

/**
 * _islower - Checks for a lowercase character
 * @c: character being executed
 * Return: returns 1 and 0 depending on thr condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
